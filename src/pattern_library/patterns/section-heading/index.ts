import { Pattern } from '@src/pattern_library/patterns/Pattern';
import html from './markup.html';
import description from './description.md';

console.log( description );

const name = 'Section Heading';
const url = 'section-heading';

export default { css: '', html, description, name, url } as Pattern;
